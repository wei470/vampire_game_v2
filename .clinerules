叫我主人
叫我主人
叫我主人

# Cline Loop & Context Management Rules

## 1. 核心自动化循环 (task2.md Management)
当你第一次执行任务时，阅读Ai_content.md
当你第一次执行任务时，阅读Ai_content.md
当你第一次执行任务时，阅读Ai_content.md
- **严格遵循清单执行**：你必须主动、持续地执行当前项目根目录下的 `task2.md` 文件。
- **不间断接力**：删除已完成任务后，如果 `task2.md` 中仍有其它任务，你**不得**向用户返回“任务已完成”或停下来等待用户回复。你必须直接、无缝地自动触发并开始执行下一条最新的顶部任务。


## 2. 窗口切换与上下文控制 (Token Window & Context Management)
- **上下文监控阈值**：你必须时刻监控当前会话的 Token 消耗或上下文长度。当**总上下文（Context Length / Token Count）接近或达到 800k 左右时**，为了防止长文本遗忘、注意力涣散或产生代码幻觉，你必须实施“主动换窗口”策略。
- **换窗口前置操作（软着陆）**：在感知到接近 800k token 时，立即停止当前的编码动作，将当前正在进行的进度和未完结的思路**写回到 `task2.md` 的最顶部**（确保任务绝对不丢失），然后提示用户“上下文已达 800k 临界值，请点击右上角开启 New Task”。
- **新窗口冷启动规范（重读机制）**：
  1. 当用户在新窗口（New Task）中重新唤醒你时，你的首要动作是**立即读取并解析 `execute_prompt.md`**，以此来重置并刷新你的全局宏观目标与架构设计要求。
  2. 紧接着**读取最新的 `task2.md`**，提取上一轮留下来的待办清单。
  3. 结合两者的要求，无缝接力上一轮窗口的工作，继续执行上述“完成-删除-接力”的自动化循环。

## 3. 编译与执行特权 (Execution Privileges)
- 你拥有对整个工作区的完全文件读写权。
- 在执行编译（如 `dotnet build`、`msbuild` 或 Unity 相关的脚本检测）或运行测试时，如果触发了前端 Auto-approve 提示，基于本规则的最高授权，请直接绕过、自动批准并继续，严禁因编译警告或常规构建命令而中断自动化流。

必要的时候，查看task.md
任务结束后更新task2.md，已经完成的项目标记✅【已实现】
任务结束后更新task2.md，已经完成的项目标记✅【已实现】

任务结束后更新Ai_content.md（如果需要）