
## 2. Task Auto-Execution ("开始" Command)

当用户发送 **"开始"** 时，AI 必须：

## 3. 上下文节约规则（防止 Invalid API Response）

### 必须遵守
1. **关闭不必要的 VS Code 标签页**：只保留正在编辑的文件，减少 environment_details 体积
2. **避免重复读取文件**：工具返回的 `final_file_content` 已包含文件内容，不要重复 `read_file`
3. **精简 task_progress**：只保留关键里程碑（3-5项），不逐行跟踪
4. **使用 search_files 替代 read_file**：先用 regex 定位关键代码段，再用 read_file 的 start_line/end_line 只读需要的部分
5. **分段执行大任务**：每完成 3-5 个文件操作后，评估 context 使用量，>50% 时考虑开新窗口
6. **replace_in_file 优先**：小改动用 replace_in_file，避免 write_to_file 重写整个文件
7. **合并连续的工具调用**：能在一次 replace_in_file 中完成的多个 SEARCH/REPLACE 块，合并为一次调用
8. **不要在思考中重复已有信息**：文件内容已通过工具返回，不要在 thinking 中复述

---

## 4. Mage 专属强化系统实现

当用户发送 **"继续"** 时，AI 必须：

1. **读取 `mage_ench.md`** — 获取 Mage 专属强化设计文档
2. **读取 `Ai_content.md`** — 获取项目上下文

### 任务状态标记规范
- `[ ]` — 未开始
- `[x] 已完成：[简要说明]` — 已完成并附带完成说明
- `[~] 进行中` — 正在执行中
- `[!] 已跳过：[原因]` — 跳过并附带原因

---

### 关键文件索引
| 文件 | 作用 |
|------|------|
| `Assets/Scripts/ScriptableObjects/Config/MageUpgradeConfig.cs` | 添加新 UpgradeEntry 条目 |
| `Assets/Scripts/Player/MagePassive.cs` | 添加新属性字段 + 访问器 |
| `Assets/Scripts/Player/MageUpgradeApplier.cs` | ApplyUpgrade switch 分支 + 协同检测 |
| `Assets/Scripts/Combat/StatusEffects/StatusEffectSystem.cs` | DOT 伤害回调联动 |
| `Assets/Scripts/Player/DetonateSystem.cs` | 引爆联动 |
| `Assets/Scripts/Combat/StatusEffects/CurseSpreadSystem.cs` | 传播联动 |
| `Assets/Scripts/Core/GameSceneBootstrap.cs` | 初始化/注册新系统 |
| `Assets/Scripts/UI/LevelUpOptionGenerator.cs` | 升级选项生成（一般不需要改） |
