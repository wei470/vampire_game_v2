## 2. Task Auto-Execution ("开始" Command)

当用户发送 **"开始"** 时，AI 必须：

## 3. 上下文节约规则（防止 Invalid API Response）

### 必须遵守
1. **关闭不必要的 VS Code 标签页**：只保留正在编辑的文件，减少 environment_details 体积
2. **避免重复读取文件**：工具返回的 `final_file_content` 已包含文件内容，不要重复 `read_file`
3. **精简 task_progress**：只保留关键里程碑（3-5项），不逐行跟踪
4. **使用 search_files 替代 read_file**：先用 regex 定位关键代码段，再用 read_file 的 start_line/end_line 只读需要的部分
5. **分段执行大任务**：每完成 3-5 个文件操作后，评估 context 使用量，>50% 时考虑开新窗口
6. **replace_in_file 优先**：小改动用 replace_in_file，避免 write_to_file 重写整个文件
7. **合并连续的工具调用**：能在一次 replace_in_file 中完成的多个 SEARCH/REPLACE 块，合并为一次调用
8. **不要在思考中重复已有信息**：文件内容已通过工具返回，不要在 thinking 中复述

---

## 4. Mage 优化任务执行

当用户发送 **"开始"** 或 **"继续"** 或 **"完成task2.md任务"** 时，AI 必须：

1. **读取 `Ai_content.md`** — 获取项目上下文（包含 V2 优化进度）
2. **读取 `task2.md`** — 读取当前优化任务清单（V2，25项）
3. **跳过已完成项**（标记 `[x]` 的），从未完成的最高优先级开始
4. **按推荐顺序执行**（见 task2.md 末尾的"待做项"表格）
5. **每完成一项**：在 task2.md 中标记 `[x] 已完成：[简要说明]`
6. **context >50% 时**：停止并提示用户开新窗口继续

### 当前已完成项（截至 2025-06-10）
- ✅ #3 独立 DOT 组件 OnEnable 缓存
- ✅ #5 BleedBullet 死代码清理
- ✅ #9 WindErosionEffect 文字缓存
- ✅ #10 WindErosionEffect.ApplyKnockback 修复
- ✅ #12 元素反应参数配置化
- ✅ #15 Glow 对象池回收

### 新窗口继续的推荐顺序
1. #2 DetonateSystem GC 消除（🟡 20min）
2. #1 DetonateSystem 遍历合并（🟡 30min）
3. #6 DotFusionSystem 审计（🟢 10min）
4. #7 DOT 子弹迁移 DotBulletBase（🟡 60min）
5. #8 MagePassive 属性 POCO（🟡 30min）
6. #13 DOT 数值配置化（🟢 30min）
7. #16 空间分区（🔴 60min，建议单独窗口）

### 新窗口触发指令
用户只需说 **"继续"** 或 **"开始"**，AI 会自动从 task2.md 的待做项继续。

### 任务状态标记规范
- `[ ]` — 未开始
- `[x] 已完成：[简要说明]` — 已完成并附带完成说明
- `[~] 进行中` — 正在执行中
- `[!] 已跳过：[原因]` — 跳过并附带原因

---

## 5. Config 实时更新机制（OnValidate → OnConfigChanged 信号刷新）

### 架构概述
所有 Config ScriptableObject 使用以下模式实现运行时实时更新：

```csharp
// Config 文件中：
public static System.Action OnConfigChanged;
#if UNITY_EDITOR
private void OnValidate() { OnConfigChanged?.Invoke(); }
#endif
```

```csharp
// 消费者组件中（如 PoisonPuddle）：
private void OnEnable()
{
    DotBulletConfig.OnConfigChanged += RefreshFromConfig;
}
private void OnDisable()
{
    DotBulletConfig.OnConfigChanged -= RefreshFromConfig;
}
private void RefreshFromConfig()
{
    _duration = DotBulletConfig.GetDefault().PoisonPuddleDuration;
}
```

### 已完成信号刷新的组件
| 组件 | 文件 | 刷新的字段 |
|------|------|-----------|
| PoisonPuddle | `Combat/PoisonBullet.cs` | `_duration` (PoisonPuddleDuration) |
| BurnStackEffect | `Combat/BurnBullet.cs` | `_duration` (BurnDuration) |
| FrostLightningField | `Combat/FrostLightningField.cs` | `_duration`, `_radius`, `_frostTickInterval` |
| MagePassive.Firing | `Player/MagePassive.Firing.cs` | 光明子弹发射后立即蓄力（LightPostFireDelay） |

### 待完成信号刷新的组件（下个窗口优先执行）
| 优先级 | 组件 | 文件 | 需要刷新的字段 | Config 字段 |
|--------|------|------|--------------|-------------|
| 🔴高 | FrostEffect | `Combat/FrostBullet.cs` | `_maxSlow`, `_baseSlow`, `_perStackSlow` | FrostMaxSlow, FrostBaseSlowPct, FrostSlowPerStack |
| 🔴高 | StaticStackEffect | `Combat/LightningBullet.cs` | 放电间隔、硬直时间、最大层数 | StaticBaseInterval, StaticStackReduction, StaticMinInterval, StaticStunOnHit, StaticStunOnDischarge, StaticStunOnFirstStack, StaticMaxStacks |
| 🔴高 | WindErosionEffect | `Combat/WindBullet.cs` | 击退距离、最大层数 | WindErosionKnockbackDistance, WindMaxStacks |
| 🔴高 | LightMarkEffect | `Combat/LightBulletController.cs` | 每层受伤加成 | LightMarkDamagePerStack |
| 🟡中 | DotBulletFactory | `Combat/DotBulletFactory.cs` | 清除 `_config` 缓存让下次 GetDefault() 重新加载 | 所有字段 |
| 🟡中 | DarkMarkEffect | `Combat/DarkBullet.cs` | 传播半径、效率、受伤加深 | DarkBaseRadius, DarkBaseEfficiency, DarkMarkDamageBonus |
| 🟢低 | LightBulletController | `Combat/LightBulletController.cs` | 蓄力/扫射参数 | LightChargeDuration, LightSweepAngle, LightSweepDuration 等 |

### 执行模式
每个组件的改造步骤一致：
1. 在 `OnEnable` 末尾添加 `DotBulletConfig.OnConfigChanged += RefreshFromConfig;`
2. 添加 `OnDisable` 方法（如已有则追加）：`DotBulletConfig.OnConfigChanged -= RefreshFromConfig;`
3. 添加 `RefreshFromConfig()` 方法，从 `DotBulletConfig.GetDefault()` 读取对应字段
4. DotBulletFactory 需要在 `OnConfigChanged` 中执行 `_config = null;` 清除缓存

---

### 关键文件索引
| 文件 | 作用 |
|------|------|
| `Assets/Scripts/ScriptableObjects/Config/DotBulletConfig.cs` | DOT子弹配置（40+字段，已中文化Tooltip） |
| `Assets/Scripts/ScriptableObjects/Config/EnemySpawnConfig.cs` | **[新]** 敌人生成配置（40+字段，8个分组） |
| `Assets/Editor/CreateEnemySpawnConfigAsset.cs` | **[新]** 创建 EnemySpawnConfig.asset 的编辑器工具 |
| `Assets/Editor/DotBulletConfigInspector.cs` | **[新]** DotBulletConfig 自定义 Inspector（中文标签） |
| `Assets/Scripts/ScriptableObjects/Config/MageUpgradeConfig.cs` | Mage升级配置（DOT枪+升级选项） |
| `Assets/Scripts/ScriptableObjects/Config/EnemyWaveConfig.cs` | 波次配置（S曲线难度、特殊波次） |
| `Assets/Scripts/ScriptableObjects/Config/GameConfig.cs` | 全局游戏配置 |
| `Assets/Scripts/Player/MagePassive.cs` | DOT枪管理+属性+子弹发射（~530行，已优化） |
| `Assets/Scripts/Player/MagePassive.Firing.cs` | Update+子弹发射+视觉（~160行，已支持光明子弹立即蓄力） |
| `Assets/Scripts/Combat/DotBulletBase.cs` | DOT子弹基类（速度/生命周期/穿透/反弹） |
| `Assets/Scripts/Combat/DotBulletFactory.cs` | DOT子弹工厂（从Config读取参数创建子弹） |
| `Assets/Scripts/Combat/StatusEffects/StatusEffectSystem.cs` | DOT效果管理（引爆+视觉） |
| `Assets/Scripts/Combat/StatusEffects/DotEffectRegistry.cs` | DOT效果统一注册表 |
| `Assets/Scripts/Player/DetonateSystem.cs` | 引爆系统（蓄力/连锁/余烬/碎裂） |
| `Assets/Scripts/Combat/StatusEffects/CurseSpreadSystem.cs` | 死亡时传播DOT |
| `Assets/Scripts/Enemies/EnemyBase.cs` | 敌人基类（移速/碰撞/LOD） |
| `Assets/Scripts/Enemies/SpawnManager.cs` | 波次管理（生成+状态） |
| `Assets/Scripts/Enemies/EliteModifierSystem.cs` | 精英修饰器（8种子能力） |
| `Assets/Scripts/Combat/FrostLightningField.cs` | 霜电冰场（元素反应） |
| `Assets/Scripts/Combat/FrostBullet.cs` | 霜冻子弹+FrostEffect |
| `Assets/Scripts/Combat/LightningBullet.cs` | 雷电子弹+StaticStackEffect |
| `Assets/Scripts/Combat/WindBullet.cs` | 风蚀子弹+WindErosionEffect |
| `Assets/Scripts/Combat/LightBulletController.cs` | 光明蓄力激光+LightMarkEffect |
| `Assets/Scripts/Combat/DarkBullet.cs` | 暗影子弹+DarkMarkEffect |
